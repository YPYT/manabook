<h2>注文確認画面</h2>
  <%= @listing.title %>
  <% @listing.list_images.each do |image|  %>
    <%= image_tag image.variant(resize_to_limit: [300,300]), alt: @listing.title %>
  <% end %>
  <%= @listing.price %>


<%= button_to "Continue", listing_order_success_path %>
<%= link_to "Cancel", listing_path(@listing) %>